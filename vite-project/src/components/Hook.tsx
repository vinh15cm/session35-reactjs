import React from 'react'

export default function Hook() {
    return (
        <div>
            Hook
            {/**
       * Hook (móc): dùng cho functional component ra đời ở phiên bản 
       * REACT 16.8(2018)
       * REACT ra đời năm 2013
       * khi hook chưa ra đời thì function component chỉ có nhiệm vụ tạo ra các UI
       * function header{
       * return(
       *         <div>header</div>
       * )
       * }
       * khi mà hook ra đời thì bản chất hook là những hàm , phương thức đưa vào , nối vào
       * trong function giúp cho function component có thể làm được tính năng như classComponent
       * việc các em dùng hook ngắn gọn hơn , không cần dùng this (khó hiển thị như trong class)
       * HOOK LÀ NHỮNG CÁI HÀM , PHƯƠNG THỨC
       * các hook mà em học
       * 1,useState
       * 2,useEffect
       * 3,memo(HOC-higher order component)
       * 4,useCallback
       * 5,useMemo
       * 6,useContext
       * 7,useReducer
       */}
        </div>
    )
}
